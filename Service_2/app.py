from application import app

if __name__=="__main__":
    app.run(port=5001, host='0.0.0.0', debug=True)                                  # Define port 5001 as the port the application will be accessible on
